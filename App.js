'use strict';
/**
 * Created by neo on 14/10/2016.
 */

/**
 * 用来注册一个小程序
 * @param {Object} options
 * @param {function} [options.onLaunch] - 生命周期函数--监听小程序初始化 当小程序初始化完成时，会触发 onLaunch（全局只触发一次）
 * @param {function} [options.onShow] - 生命周期函数--监听小程序显示 当小程序启动，或从后台进入前台显示，会触发 onShow
 * @param {function} [options.onHide] - 生命周期函数--监听小程序隐藏 当小程序从前台进入后台，会触发 onHide
 */
function App(options) {}

/**
 * 获取当前页面的实例
 * @return {Page}
 */
App.prototype.getCurrentPage = function () {}

/**
 * 获取到小程序实例
 * @return {App}
 */
function getApp() {}
